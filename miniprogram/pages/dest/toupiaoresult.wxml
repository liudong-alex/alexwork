<!--demo.wxml-->
<view class="uploader">
    <view class="uploader" wx:for="{{resultPros}}" wx:key="index" wx:for-item="resultPro">
        <text class='uploader-text'>{{resultPro.name+'：'+resultPro.count+'票'}}</text>
    </view>
    <!-- <view style="display: flex; flex-direction: column; padding: 40rpx;">
        <button bindtap="backIndex">返回主页</button>
    </view> -->
    <button wx:if="{{auth === 'admin'}}" bindtap="tabResult" class='uploader-text'>查看投票详情</button>
</view>